# 项目交接说明

更新时间：2026-06-28 19:05 Asia/Shanghai

## 1. 项目目标

当前项目目标始终没有变化：

- 保住原图本人身份，不做整脸替换
- 迁移参考图发型
- 迁移参考图妆容
- 尽量保留原图眼镜、发箍等配饰

用户当前最关注的不是“生成看起来更好一点”，而是：

- 人脸面部结构和五官保持住
- 发型和妆容按参考图真实迁移
- 不能接受“保脸成功，但妆发迁移失败”

## 2. 当前系统真实状态

### 2.1 当前存在的两条链路

1. `ark_hybrid_mainline`
- 真实调用 Ark 公开主链路能力
- 输入是 `source + reference`
- 目前能真实出图
- 但当前公开能力仍属于 hybrid，不是已确认的 Ark 服务端原生双分支可执行控制

2. `two_stage_local_edit`
- 兜底链路
- 基于 `jimeng_image2image_dream_inpaint`
- 分两阶段：
  - `hair_only`
  - `makeup_only`
- 目前这条链路经常被 provider 侧错误打断

### 2.2 当前已经完成的关键工程工作

- Ark 主链路已从“占位 worker”升级为真实可调用链路
- 参考图解析器 V2 已接入
- 预处理真实 mask 已接入并落盘
- `visual_identity` 模式已接入
- visual 模式下的配饰保护和后处理选择逻辑已优化
- `transfer_score` 已从纯启发式改为“候选图再次解析后的真实妆发比对”
- `visual_identity` 已加入内部 oversampling
- 候选拒绝逻辑已加入：
  - `updo_missing`
  - `bangs_missing`
  - `side_locks_missing`
  等硬失败

### 2.3 当前没有完成的关键能力

- 参考图解析器还不是细粒度语义解析器
- prompt schema 还过于粗糙
- Ark public global 仍无法稳定产出目标盘发结构
- `hybrid_makeup_refine_stage` 经常 `Access Denied`
- `two_stage_local_edit` 经常出现：
  - `Access Denied`
  - `cannot identify image file`
- 仍没有真正可用的“解析正确 -> prompt 正确 -> 稳定生成正确”的闭环

## 3. 最近两轮对话后的核心结论

### 3.1 不是“生成数量不够”，而是“参考图理解错了”

用户已经明确指出：

- 即使增加到 8 张、100 张
- 如果生成方向本身错了
- 只是得到更多错误结果

这个判断是正确的。

当前更核心的问题不是采样数，而是：

- 参考图发型和妆容信息提取不准
- 提取后的 prompt 又被压扁
- 生成模型收到的是错误或过粗的目标描述

### 3.2 参考图 `reference.png` 的妆容提示词当前确实提错了

当前解析结果里：

- 眼影被提成 `warm_brown + deep_brown`
- 眉色被提成 `deep_brown`
- 唇色被提成 `soft_beige`

但用户基于参考图肉眼判断为：

- 上眼皮偏粉色
- 下眼皮偏米棕色
- 眉毛是灰棕色
- 唇妆更接近珠光杏粉色

因此问题不是翻译错误，而是上游提取本身错误。

### 3.3 发型结构字段相对更接近，但仍有粗糙和错误

当前发型结构字段里，以下内容基本合理：

- `updo_with_bangs`
- `soft_bun`
- `middle`
- `soft_bangs`
- `side_locks.exists=True`

但以下字段仍不够准：

- `texture=wavy`
- `hair_color=natural_brown`
- `finish=natural`

说明当前解析器在“几何结构”上比妆容略强，但在发色、发面质感等方面仍然粗糙。

## 4. 当前问题的根因判断

## 核心判断

当前最主要的问题不是“颜色词库太少”，而是：

- 参考图解析机制本身不具备可泛化的细粒度语义识别能力

### 4.1 当前参考图解析器的真实工作方式

当前 `reference_parser.py` 主要是：

- 先用启发式区域切分
- 再对区域取中位色、亮度、饱和度、纹理统计
- 最后用少量阈值规则映射到有限标签

它不是：

- 真正的 VLM 多模态理解
- 专业妆容属性识别模型
- 色号识别模型
- 眉型/眼妆分层识别模型

### 4.2 为什么它识别不准

1. 区域太粗
- 眼妆现在主要看合并后的 `eye_band`
- 没有拆：
  - 上眼皮
  - 下眼皮
  - 眼尾加深
  - 卧蚕
  - 珠光/哑光区域

2. 颜色表达太粗
- 当前只是把综合色调映射到少量颜色标签
- 不是在识别真实的妆容色系、质地和商业色号

3. 弱信息也会被强行输出
- 例如修容、高光、眉型即使不明显，也常被硬填一个标签
- 没有完善的 `unknown / low_confidence` 输出策略

4. prompt schema 二次丢信息
- 即使上游提取到一点信息
- 最终也只会压成：
  - `eyeshadow main/secondary`
  - `brows shape/color`
  - `lips color`
- 不足以表达复杂妆容

### 4.3 结论

所以当前问题应该表述为：

- 不是“某几个颜色词没加进去”
- 而是“当前参考图解析器 + prompt schema 这整套表达机制太粗，天然会持续提错”

## 5. 最近真实验证结果

### 5.1 `visual_identity` oversampling 和硬评分已生效

已验证任务：

- `job_69ca1bf2a62c4bf1b7c6b629c76516ed`
- source: `D:\水木年华\测试图\origin.jpg`
- reference: `D:\水木年华\测试图\reference.png`

真实结果：

- `visual_identity` 请求 1 张时，内部已实际生成 4 张 Ark global base 候选
- 4 张候选全部被再次解析
- 4 张候选全部仍是 `down + side_6_4 + no side locks`

结论：

- 这次失败不是评分误判
- 而是 Ark global 这轮根本没有生成出目标盘发主结构

### 5.2 当前新的失败点已经很清楚

当前失败点是：

1. 上游解析器对妆容理解不准
2. prompt 表达过粗
3. Ark global 没被描述成足够明确、可执行的目标
4. fallback refine 链路又不稳定，无法可靠补救

## 6. 下一步应该做什么

## 最高优先级结论

下一步最应该做的不是继续加 oversampling，也不是先补大词库，而是：

### P0：重做参考图解析器

目标：

- 从“粗区域 + 平均色 + 阈值标签”升级为“细粒度结构化属性提取”

为什么先做这个：

- 当前所有生成目标都建立在解析结果上
- 解析错了，后面所有生成都会朝错误方向跑
- 不先修这个，继续调 prompt / 候选数 / retry 都是在放大错误输入

第一批必须重做的字段建议：

1. 发型
- 主结构：`updo / half_up / down`
- 盘发类型：`soft_bun / tight_bun / ponytail / ...`
- 刘海类型
- 分线
- 两侧碎发存在与强度
- 发面质感：`sleek / natural / fluffy`
- 发色深浅和冷暖

2. 眉毛
- 颜色
- 眉型
- 浓度
- 拱度

3. 眼妆
- 上眼皮颜色
- 下眼皮颜色
- 眼尾加深色
- 眼线类型
- 卧蚕/提亮
- 材质：`matte / satin / shimmer`

4. 唇妆
- 主色
- 冷暖
- 明度
- 饱和度
- 质地：`matte / satin / glossy / pearly`

5. 底妆
- 光泽度
- 粉感
- 遮瑕感
- 均匀度

特别注意：

- 必须允许输出 `unknown`
- 不要在低置信度时强行给标签

### P0：重做 prompt schema

目标：

- 让 prompt 能承载新的细粒度解析结果

为什么必须和解析器一起做：

- 否则上游改好了，下游 prompt 还是会把信息压扁

建议：

- 不再只写 `eyeshadow main/secondary`
- 要能表达：
  - `upper_lid_color`
  - `lower_lid_color`
  - `lip_finish`
  - `brow_tone`
  - `hair_surface_finish`
  - `bun silhouette`
  - `side_locks`

### P1：修复 fallback 链路稳定性

目标：

- 解决 `hybrid_makeup_refine_stage` 的 `Access Denied`
- 解决 `two_stage_local_edit` 的 `cannot identify image file`
- 解决 0 字节中间输出

为什么排在 P1 而不是 P0：

- 这条链路确实需要修
- 但当前主要矛盾已经不是“没有兜底链路”，而是“目标本身没描述对”

### P2：再回头调主链路生成约束

在解析器和 prompt schema 改完之后，再做：

- mainline prompt 强化
- candidate 策略微调
- oversampling 数量调节
- visual / strict 模式边界优化

否则现在继续调这些，收益有限。

## 7. 现在不建议优先做什么

### 7.1 不建议先做大而全发型库

原因：

- 发型库解决的是“标准化表达”
- 不是“从图片里看对”

### 7.2 不建议先做大而全色号库

原因：

- 如果解析器还是粗区域平均色
- 色号库越大，只会把错误结果映射得更精细

### 7.3 不建议继续只加候选数

原因：

- 如果目标方向仍错
- 只是得到更多错误样本

## 8. 如果后续开始改造，执行时要注意什么

1. 先修“识别层”，再修“词典层”
- 先让系统能分清属性
- 再做标准词库映射

2. 先保留连续特征，再映射标签
- 不要直接从图片硬猜词
- 先输出连续值和局部属性

3. 对低置信度字段允许留空
- `unknown` 比胡猜一个错误标签更有价值

4. prompt 不要再一次压扁信息
- 解析字段必须尽量结构化透传

5. 保持验证闭环
- 每次修改解析器后，都至少要验证：
  - `reference.png`
  - 一张明显不同风格的参考图
- 避免只对当前样本过拟合

6. 继续保留真实候选再解析评分
- 即使后面解析器重做，这个闭环仍有价值
- 它能帮助判断“生成是否真的更接近参考图”

## 9. 建议优先级

### P0
- 重做参考图解析器
- 重做 prompt schema

### P1
- 修复 fallback 稳定性
- 解决 `Access Denied`
- 解决 `cannot identify image file`

### P2
- 基于新解析器重新调主链路 prompt
- 重新评估 visual 模式和 strict 模式
- 再决定是否继续增加 oversampling

### P3
- 做小而稳定的发型标准库
- 做小而稳定的妆容色彩标准库

注意：

- 库应作为“标准化输出层”
- 不是“主识别层”

## 10. 关键文件

- [app/services/reference_parser.py](/D:/水木年华/app/services/reference_parser.py)
- [app/services/model_clients.py](/D:/水木年华/app/services/model_clients.py)
- [app/services/orchestrator.py](/D:/水木年华/app/services/orchestrator.py)
- [app/services/scoring.py](/D:/水木年华/app/services/scoring.py)
- [app/services/postprocess.py](/D:/水木年华/app/services/postprocess.py)
- [PROJECT_MEMORY.md](/D:/水木年华/PROJECT_MEMORY.md)

## 11. 一句话交接结论

当前项目已经完成了不少工程闭环，但真正卡住结果质量的主因已经很明确：

- 不是候选数不够
- 不是简单少几个颜色词
- 而是参考图解析器和 prompt schema 还不具备细粒度、可泛化的妆发语义表达能力

下一步最应该优先做的是：

- 先重做参考图解析和 prompt 表达
- 再修 fallback
- 最后再继续调主链路生成策略
