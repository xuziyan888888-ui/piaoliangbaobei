# 项目交接说明

更新时间：2026-06-27（Asia/Shanghai）

## 1. 项目目标

本项目目标始终没有变化：

- 保住原图本人身份，不做换脸
- 迁移参考图发型
- 迁移参考图妆容
- 尽量保留眼镜、发箍等原图配饰

目标效果参考“参考图妆发迁移”，不是 `volc-faceswap` 这类整脸替换。

## 2. 当前项目到哪里了

### 2.1 已经跑通的链路

项目当前已经同时具备两条链路：

1. Ark 主链路
   - 已修复此前的 `no such api`
   - 当前通过火山视觉异步任务接口提交
   - 主链路已经可以真实发起请求并返回结果
2. two_stage_local_edit 兜底链路
   - 基于 `jimeng_image2image_dream_inpaint`
   - 已跑通 `hair_only -> makeup_only` 两阶段局部编辑

### 2.2 最近一次主链路真实烟测

已完成一次真实主链路烟测，确认“主链路不是空壳占位”：

- source：`D:\水木年华\测试图\origin2.jpg`
- reference：`D:\水木年华\测试图\reference7.jpg`
- output：`D:\水木年华\outputs\job_e8ed721b27e74b7484c6e299926e407d_global_0.png`
- 结论：`selected_pipeline=ark_complete_mainline`，没有回退到 `two_stage_local_edit`

### 2.3 当前已经完成的工程模块

- 参考图解析 V2 已接入，能输出较丰富的妆发结构化描述
- 预处理已能落盘真实 mask，不再是 mock
- 主链路已经支持双图输入 `source + reference`
- inpaint 兜底链路已修正为 `source + active_edit_mask`
- 工程里已经区分“主链路”和“兜底链路”两个概念

## 3. 当前真实结论

### 3.1 Ark 仍然是主链路平台

当前正确口径不是“Ark 不是主链路”，而是：

- Ark 仍然是目标主链路平台
- 但当前仓库还没有完整复现“Ark 原生完整妆发迁移流水线 + 工程控制闭环”

### 3.2 当前主链路能跑，但质量还不达标

当前主链路的状态是：

- 发型迁移已经能出现明显效果
- 但人脸身份仍会漂移
- 眉毛、眼妆会被过度重绘
- 妆容不是“按参考图精确迁移”，而更像泛化美化

### 3.3 当前仓库离目标还差的不是一个参数

已经验证，当前问题不是简单的 prompt、mask 或两阶段顺序问题，而是主链路缺少一整套真正生效的控制能力，包括：

- 真身份编码
- 可执行的局部控制输入
- 真正的锁脸闭环
- 后处理像素回填
- 真质检与自动重试

## 4. 当前核心问题是什么

### 4.1 锁脸信息没有作为真正可执行输入送进主链路

现在主链路虽然会生成 `face_lock_mask`、`editable_makeup_mask`、`accessory_mask`，但这些信息主要被写进 `req_json` 文本说明里，并没有作为 Ark 主链路的硬控制输入真正参与生成。

直接结果就是：

- 发型可以学到一部分
- 脸型、眉眼、皮肤也会被一起重画

### 4.2 identity embedding 还是伪实现

当前所谓 identity embedding 不是 ArcFace/AdaFace 这类真实身份向量，而是从 mask 派生出的伪向量预览，不能承担“保住本人脸”的职责。

这意味着当前 `identity_lock_strength=0.95` 更多还是软提示，不是真身份锁定。

### 4.3 主链路 prompt 对眉毛和眼妆约束过强

当前主链路 prompt 会明确要求迁移眉毛、眼影、眼线等特征，但没有足够限制“不得重塑五官”。  
所以现在出现的粗眉、眼线变重、眼部风格跑偏，并不完全是随机错误，而是当前 prompt 策略本身就在推动模型这么做。

### 4.4 后处理基本没有落地

当前代码还没有真正实现以下闭环：

- 配饰像素回填
- 锁脸区域像素回填
- 边缘融合
- 超分修复

所以即使主链路生成结果局部可用，也没有后处理把“该保留的原图内容”贴回去。

### 4.5 质检分还不是真看图

当前 scoring 仍偏启发式，不能可靠识别“发型好但改脸”的失败结果，因此还没有形成自动拦截和自动重试能力。

## 5. 为什么会出现“发型不错，但人脸和妆容不对”

这是当前系统的典型现象，原因可以概括为：

1. 主链路已经拿到了 source + reference，所以发型风格能开始迁移
2. 但锁脸、配饰保护、局部妆区控制没有变成可执行硬约束
3. identity embedding 不是有效的人脸身份特征
4. prompt 又显式推动了眉毛和眼妆强迁移
5. 后处理没有把原图脸部和配饰像素贴回去

因此现阶段最常见的输出就是：

- 发型方向对了
- 但脸被“美化改写”
- 眉毛和眼妆比目标更重
- 最终不满足“保本人 + 迁移参考妆发”

## 6. 已完成工作清单

- 修复 Ark 主链路 `no such api`
- 确认主链路真实可跑，不再是占位 worker
- 完成一次主链路烟测并拿到真实结果图
- 接好两阶段 inpaint 兜底链路
- 接入参考图解析 V2
- 接入并落盘真实预处理 mask
- 完成主链路与兜底链路的分层认知

## 7. 还缺哪些关键能力

这是当前最重要的缺口列表，也是后续研发主任务：

- RetinaFace 真实人脸检测链
- BeautySeg 级别的人像语义分割
- ArcFace / AdaFace 真实身份编码
- Avatar3D / 3D face geometry 注入主干
- 真正双分支解耦生成控制
- 主链路可执行 mask / control 输入
- 后处理像素回填 / 融合 / 超分
- 真实身份质检与自动重试

## 8. 下一步最应该做什么

建议按下面顺序推进，不要再把主要精力放在“继续微调当前 prompt”上。

### P0：先把“锁脸控制”从文本备注变成真控制

目标：

- 明确 Ark 主链路到底支持哪些真正可执行的 control 输入
- 把当前 `face_lock_mask`、`editable_makeup_mask`、`accessory_mask` 从“req_json 描述”升级为“真实控制输入”

验收标准：

- 主链路请求结构里能看到硬控制输入，而不只是文字说明
- 同一组图重复跑时，人脸漂移明显下降

### P0：接入真实身份编码和身份质检

目标：

- 用 ArcFace 或 AdaFace 替代当前伪 identity embedding
- 用真实人脸相似度校验做质检
- 相似度不达标时自动重试或降级兜底

验收标准：

- 质检分与肉眼判断一致性明显提升
- 能稳定拦截“发型好但改脸”的结果

### P1：收紧主链路妆容控制范围

目标：

- 重写主链路 prompt 策略
- 把眉形、眼线、眼影从“强迁移”改成“受限迁移”
- 区分“允许迁移的妆效”和“不得重塑的五官结构”

验收标准：

- 眉毛不再被大幅改形
- 眼妆更接近参考图，同时不破坏原图眼部结构

### P1：落地后处理回填闭环

目标：

- 实现 accessory 像素回填
- 实现 face lock 区域原图回填
- 做边缘融合、必要时超分

验收标准：

- 眼镜、发箍等配饰不再丢失
- 发际线、额头、脸部边缘过渡更自然

### P2：补齐更高等级预处理

目标：

- 提升人脸关键点、语义分割、遮挡修复质量
- 为后续主链路控制提供更可靠的区域资产

## 9. 当前不建议做的事

在上述 P0 完成前，不建议继续把主要时间投入到：

- 单纯微调当前主链路 prompt
- 继续盲调 `identity_lock_strength`
- 只优化 two_stage_local_edit 的表现并把它当主链路
- 用启发式分数继续判断“是否保住本人脸”

这些动作最多只能优化当前效果，不能解决主链路缺核心控制的问题。

## 10. 关键代码文件

- `app/services/model_clients.py`
- `app/services/preprocess.py`
- `app/services/generator.py`
- `app/services/orchestrator.py`
- `app/services/postprocess.py`
- `app/services/scoring.py`
- `app/services/reference_parser.py`
- `app/config.py`
- `.env.local`

## 11. 关键产物与参考文档

关键产物：

- 主链路烟测结果：`D:\水木年华\outputs\job_e8ed721b27e74b7484c6e299926e407d_global_0.png`
- 近期 inpaint 结果：`D:\水木年华\outputs\job_7a581ed43e7341a78b0eca121046c8e0_local_0_makeup_stage_0_job_7a581ed43e7341a78b0eca121046c8e0_local_0_hair_stage_0.png`
- mask 输出目录：`D:\水木年华\outputs\preprocess_masks`

参考文档：

- `D:\水木年华\Ark成功链路对齐核查.md`
- `D:\水木年华\Ark完整主链路研发任务单.md`
- `D:\水木年华\Reference Parser V2设计.md`

## 12. 交接结论

一句话总结：

项目现在已经从“主链路根本跑不起来”推进到了“Ark 主链路已经能真实跑通并出图”，但离产品目标还差最后一层最关键的能力闭环，即真正的锁脸控制、真实身份编码、可执行局部控制输入、后处理回填和真实质检重试。

后续研发不要再把重点放在“继续盲调现有生成效果”，而要集中补齐主链路控制闭环，让系统真正具备“保住本人脸 + 完整迁移参考妆发”的工程能力。
