# 更像豆包的参考图改造路线

更新时间：2026-06-30 Asia/Shanghai

## 1. 当前问题

现在的主问题不是“颜色词不够多”，而是“生成主要依赖文字解析，视觉细节损失太大”。

具体表现：

- 文字解析很难 100% 准确。
- 眼影范围、腮红边界、唇面反光、发丝卷度、盘发体积这些连续视觉信息，很难完整写成文字。
- 现在生成结果的大类颜色经常是对的，但整体不像参考图本身的妆发质感。

这说明当前链路更像：

`reference -> 解析成长文案 -> 生成`

而不是更合理的：

`source + 参考图视觉条件 + 局部视觉条件 + 少量结构字段 + 保留约束 -> 编辑式生成`

## 2. 目标路线

目标不是让模型“不要看参考图”，而是：

- 继续让生成环节看参考图。
- 参考图主要提供妆发风格，不负责替换原图人物长相。
- 文字不再承担全部细节表达，只做规则约束和补充说明。

最终希望系统更接近这条路线：

- `source`：负责本人长相和身份
- `reference`：负责整体妆发风格
- `reference region assets`：负责局部细节
- `structured fields`：负责粗粒度语义约束
- `scoring`：负责挑更像参考图、同时仍保本人脸的结果

## 3. P0

P0 的目标是把“文字主导”改成“视觉条件主导，文字辅助”。

### P0.1 参考图继续保留整图输入

不要取消整张参考图输入。

原因：

- 整图里有发型整体轮廓、刘海布局、妆面协调关系。
- 这些信息只靠局部 patch 或文字都不完整。

需要做的不是删掉整图，而是明确它的角色：

- 整图只提供整体妆发风格。
- 原图脸部身份仍由 source 主导。

### P0.2 把 region assets 提升为主条件

当前仓库已经有：

- `hair_patch`
- `bangs_patch`
- `upper_lid_patch`
- `lower_lid_patch`
- `outer_corner_patch`
- `brows_patch`
- `lips_patch`
- `cheeks_patch`
- `complexion_patch`

下一步不要只把它们当调试产物，而要在生成链路里提高权重。

建议：

- 发型阶段优先使用 `hair_patch + bangs_patch`
- 妆容阶段优先使用 `upper_lid/lower_lid/outer_corner/brows/lips/complexion`
- prompt 中明确写“优先参考局部区域视觉证据，而不是仅依赖颜色名词”

### P0.3 解析器降级为粗粒度结构器

不要再要求 parser 把所有细节都翻译对。

parser 更适合保留这些粗粒度信息：

- 发型主结构：`updo / half_up / down / braided_pigtails`
- 刘海主类型：`airy_side_bangs / soft_bangs / side_swept_bangs`
- 发色大类：`black / dark_brown / warm_brown`
- 眉色冷暖大类
- 唇色大类
- 底妆光泽大类

不再让 parser 承担这些高风险细节：

- 眼影具体铺法的完整视觉重建
- 腮红边界形状
- 唇峰和内深外浅过渡强度
- 鬓角精确卷度

### P0.4 prompt 重新分层

prompt 要改成三层：

1. 身份保护层
- 保留原图人物长相
- 不改五官几何
- 不替换脸型

2. 风格迁移层
- 参考图迁移发型和妆容
- 参考图只作为风格板，不作为身份来源

3. 局部细节层
- 优先参考刘海、眼妆、眉毛、唇妆、底妆 patch
- 结构字段只做补充说明

也就是说，文字以后主要负责“解释规则”，不是“完整描述视觉细节”。

## 4. P1

P1 的目标是让生成链路真正消费这些视觉条件。

### P1.1 多图输入策略

如果 provider 支持多图输入，建议拆成：

- 图 1：source
- 图 2：reference full image
- 图 3：hair/bangs crop
- 图 4：eye makeup crop
- 图 5：lips/brow/complexion crop

如果 provider 不支持多图强控制，则退回分阶段方案：

- hair stage：看整图 reference + hair/bangs patch
- makeup stage：看整图 reference + eye/lips/brow/complexion patch

### P1.2 局部区域优先级

生成端需要显式定义优先级：

- 发型结构优先看 `hair_patch / bangs_patch`
- 眼妆分布优先看 `upper_lid/lower_lid/outer_corner`
- 唇面质感优先看 `lips_patch`
- 底妆肤色优先看 `complexion_patch`

不要继续把这些细节压缩成一句 `soft_pink` 或 `dusty_rose` 就结束。

### P1.3 评分也改成视觉对齐

现在评分不能只看 parser 二次解析后的标签一致性。

建议新增区域视觉相似度：

- hair silhouette similarity
- bangs layout similarity
- eye makeup color-distribution similarity
- lip color and gloss similarity
- complexion tone similarity

这样评分会更像“参考图是不是那个样子”，而不是“文字字段是不是对上了”。

## 5. P2

P2 的目标是让系统更稳，而不是只在单张图上有效。

### P2.1 评测集扩展

继续扩 reference eval：

- 高盘发
- 半扎
- 双麻花辫
- 强色彩眼妆
- 清淡裸妆
- 红唇

每类至少补几张。

### P2.2 失败类型拆分

把失败分成几类分别优化：

- 发型结构对，但妆容不像
- 妆容颜色对，但分布不像
- 本人长相保住，但参考风格弱
- 风格强，但不够像参考图局部细节

### P2.3 低置信度交互

未来产品侧不应该让用户给整张图手写完整文案。

更合理的是：

- 系统先自动解析
- 只对低置信度字段让用户轻量修正
- 例如只让用户选：
  - 刘海更偏哪种
  - 唇色更偏哪种
  - 眼影更偏粉还是棕

## 6. 对当前仓库的直接落地建议

按当前代码结构，建议执行顺序是：

1. 保留 `reference_image` 输入
2. 强化 `reference_region_assets` 在 prompt 和 transport 中的角色
3. 弱化 parser 长文案主导地位
4. 给生成阶段增加“局部视觉证据优先”约束
5. 给评分增加区域视觉相似度
6. 扩 reference eval，不再只看单样本

## 7. 结论

如果要更接近豆包这类体验，核心不是继续堆更长的描述词，而是：

- 让模型继续看参考图
- 让模型更多使用局部视觉条件
- 让文字只负责规则
- 让评分直接比较视觉区域是否像参考图

一句话总结：

从“参考图翻译成文字再生成”，升级为“参考图视觉条件主导、文字辅助约束、局部 patch 强化细节”的编辑式生成。
