# Ark 成功链路对齐核查

更新时间：2026-06-27 Asia/Shanghai

## 1. 这份核查要回答什么

这次核查不再回答“Ark 是不是主链路”，而是回答两个更准确的问题：

1. 你之前成功实现“保住本人脸 + 完整迁移妆发”的底层，到底是不是 Ark 体系
2. 当前仓库实际打到的 Ark 能力，和你成功时那条 Ark 完整流水线，到底差在哪

基于你本次补充的成功经验、产品侧架构说明和截图，当前统一口径应当是：

- `Ark` 仍然是主链路平台
- 你之前成功靠的不是“离开 Ark 的别的体系”
- 但当前仓库只命中了 Ark 体系里的一个公开 `inpaint` 子能力
- 当前仓库没有复现你成功时那套“Ark 完整流水线原生能力 + 针对性工程控制”

## 2. 先把结论讲清楚

## 核心结论

### 结论一：Ark 仍然应该被视为主链路

这点不应再被表述成“Ark 不是主链路”。

更准确的说法是：

- 主链路平台：`Ark`
- 当前仓库实际命中的能力：`Ark` 的公开 `jimeng_image2image_dream_inpaint`
- 你成功时依赖的能力：`Ark` 页面 / 产品原生底层的完整妆发迁移流水线能力

### 结论二：当前仓库没有打到你成功时那条“完整 Ark 链路”

当前仓库虽然也在调 Ark，但只是在调用一个简化的局部重绘子能力。

所以当前真正的问题不是：

- “是不是 Ark”

而是：

- “是不是 Ark 完整流水线原生底层”

答案是：

- 你成功那次：是
- 当前仓库：不是

### 结论三：当前仓库与成功链路的差异，不只是参数问题

当前仓库和你成功时的差异，至少分两层：

1. 底层能力层差异
   当前仓库命中的是公开 `inpaint` 子能力，不是完整 Ark 原生妆发迁移流水线
2. 工程控制层差异
   当前仓库缺少完整预处理修复、身份锁定流、结构化强约束、动态权重、自适应后处理、相似度过滤闭环

所以它不是“再调一调 prompt / mask / 参数就能等价复现”的问题。

## 3. 第一部分：你能同时保住人脸 + 完整迁移妆发，靠的是什么

这一部分按你本次补充的成功链路描述来统一表述。

注意：

- 下述“完整流水线模型架构”是当前产品侧工作假设
- 它和你成功样例、页面表现、产品说明是一致的
- 但其中具体内部模型名和实现细节，当前仓库还没有独立代码证据完全复原

### 3.1 不是单一模型，而是一整套 Ark 完整流水线

你成功的关键，不是单独某个“图片生成模型”突然变强，而是 Ark 页面原生底层在跑一套多模块协同流水线：

1. 前置 CV 预处理模型组
2. 结构化参考解析模型
3. 身份 / 风格双路解耦的主干生成模型
4. 后处理修复模型组
5. 人脸相似度校验与自动过滤闭环

也就是说，你成功靠的是：

- `Ark 完整流水线原生底层`
- 再叠加一套针对性的工程控制手段

而不是“裸调用一个开放 API 就直接做到”

### 3.2 预处理决定“哪些地方必须保，哪些地方允许迁移”

按你的成功链路描述，真正关键的不是先生成，而是先做精确区域解耦：

- 身份区域：五官、脸型、身份核心结构，全程锁死
- 妆造区域：头发、彩妆局部，允许参考图迁移
- 配饰区域：眼镜、发箍等强保护，后处理直接回填原图像素

这一步的意义是：

- 不是让模型自己“猜哪里该改”
- 而是先从工程上硬定义“保留 / 修改 / 回填”的边界

这正是当前仓库和成功链路的第一个本质差异。

### 3.3 主干生成不是单分支，而是“身份 / 风格双分支解耦”

按你给出的说明，成功链路真正有效的核心，不是简单的“参考图 + prompt”，而是：

- 身份分支只读原图
- 风格分支只读参考妆发
- 两条分支在不同区域用不同权重融合

五官、眼镜区域更偏身份分支；
头发、嘴唇、腮红等妆造区域更偏风格分支。

这解释了为什么你能同时做到：

- 人还是本人
- 妆发又能完整迁过去

因为这不是单分支模型做全图折中，而是先结构上拆开，再局部融合。

### 3.4 后处理和身份校验不是可选项，而是成功闭环的一部分

你成功链路里真正重要的还包括：

- 配饰区域像素直接回填
- 光影统一与边缘融合
- 生成后再做人脸相似度校验
- 不达标就自动过滤或重生成

这意味着你成功靠的不是“一次出图正好撞对”，而是一个带校验与兜底的完整闭环。

## 4. 第二部分：为什么当前仓库虽然也在调 Ark，但效果完全不同

### 4.1 当前仓库实际命中的只是 Ark 的公开 inpaint 子能力

当前代码的真实调用在 [`app/services/model_clients.py`](/D:/水木年华/app/services/model_clients.py:192)。

已经能确认的事实是：

- `.env.local` 当前用的是 `ARK_INPAINT_MODEL=jimeng_image2image_dream_inpaint`
- 图像输入是 `source_image + active_edit_mask`
- `reference_image` 没有作为模型第二输入图真正送进当前 inpaint 通道
- 当前 `global_reference` 仍然没有真实实现

直接证据：

- [`.env.local`](/D:/水木年华/.env.local:26)
- [`app/config.py`](/D:/水木年华/app/config.py:110)
- [`app/services/model_clients.py`](/D:/水木年华/app/services/model_clients.py:202)
- [`app/services/model_clients.py`](/D:/水木年华/app/services/model_clients.py:227)
- [`app/services/model_clients.py`](/D:/水木年华/app/services/model_clients.py:245)
- [`app/services/model_clients.py`](/D:/水木年华/app/services/model_clients.py:270)
- [`app/services/generator.py`](/D:/水木年华/app/services/generator.py:20)
- [`app/services/generator.py`](/D:/水木年华/app/services/generator.py:88)

### 4.2 当前仓库缺的是“完整 Ark 页面原生流水线”而不是“Ark 平台本身”

所以当前最准确的差异表述应当是：

- 不是 `Ark vs 非 Ark`
- 而是 `Ark 完整原生流水线 vs Ark 公开 inpaint 子能力`

这两者虽然同属 Ark 体系，但不是同一个工程层级。

### 4.3 当前仓库少了哪些关键层

和你成功链路相比，当前仓库至少缺这些层：

1. 完整的前置修复与高精分区
   当前虽然已有真实 mask 落盘，但还没达到产品级身份 / 妆造 / 配饰硬解耦
2. 真正的身份锁定流
   当前只有 prompt、mask、face_lock 标记，没有独立的稳定 identity branch 注入机制
3. 真正的参考图强输入
   当前参考图更多是结构化描述 + prompt 语义约束，不是完整风格分支输入
4. 动态区域权重
   当前没有产品级的身份 / 风格区域动态融合机制
5. 完整后处理闭环
   当前缺少系统化的像素回填、无缝融合、光影对齐、相似度过滤重试

### 4.4 为什么这不是“再补一点参数”能解决的

如果当前问题只是参数没调对，那么前提应该是：

- 已经打到了正确的底层能力形态

但现在仓库里的事实是：

- 命中的只是 `inpaint`
- 强参考输入没有真正进入底层生成主干
- 身份 / 风格双路解耦没有在仓库层面真实实现

所以不能把当前问题简单归因为：

- 风格权重太低
- prompt 不够强
- mask 还不够好

这些会影响效果，但解释不了“为什么你页面上能成，而仓库复现不出来完整结果”。

## 5. 当前仓库与成功链路的正确分层

从现在开始，项目里应该统一用下面这套分层：

### 主链路

`Ark 完整流水线原生底层`

它的目标是：

- 保住原图身份
- 强迁移参考图发型
- 强迁移参考图妆容
- 保留配饰
- 通过后处理和身份校验形成闭环

### 兜底链路

`jimeng_image2image_dream_inpaint + two_stage_local_edit`

它的定位是：

- 局部编辑兜底
- 发丝 / 彩妆局部修补
- 极端场景下做精细修补

但它不应再被视为主链路等价物。

## 6. 对下一步开发的直接影响

### 6.1 当前不应再继续争论“Ark 是不是主链路”

这个问题现在应该视为已经解决：

- `Ark` 是主链路平台

真正要解决的是：

- 当前代码如何对齐到 `Ark` 完整流水线原生底层

### 6.2 当前最该做的不是继续盲调出图参数

下一步不建议把精力继续主要放在：

- 微调当前 inpaint prompt
- 继续只补局部 mask
- 继续只改 `two_stage_local_edit`

因为这些只会优化兜底链路，不会把它升级成完整主链路。

### 6.3 当前最该做的事情

下一步应该围绕“完整 Ark 主链路对齐”展开，优先级如下：

1. 明确成功链路在 Ark 体系中的能力形态
   是页面原生编排、内部双分支模型、还是公开能力组合
2. 明确当前仓库与成功链路之间缺失的模块层
   不是只缺一个参数，而是缺哪些处理阶段
3. 决定仓库改造方向
   是补一个真正的 `global_reference` worker，还是明确把当前仓库保留为兜底链路实现

## 7. 一句话统一口径

一句话可以这样说：

你之前成功靠的是 `火山 Ark 完整流水线原生底层 + 一套针对性工程控制`，而当前仓库虽然也在调 Ark，但只打到了公开 `inpaint` 子能力，缺少完整预处理、身份锁定、双分支解耦、动态权重、后处理回填和相似度过滤闭环，所以两者不是同一个工程层级。
